#include <string>

using namespace std;


class Analyser {
    public:
        Analyser(string name) {
            this->name = name;
            return;
        }
    private:
        string name;
};
